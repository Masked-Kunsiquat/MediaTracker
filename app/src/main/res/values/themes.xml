<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Compose-only app: this framework theme exists solely to satisfy the manifest's
         android:theme requirement (window background / splash before Compose takes over).
         All actual theming (colors, typography, dark mode, dynamic color) is done in
         Kotlin via MediaTrackerTheme (ui/theme/Theme.kt) — no AppCompat/Material XML theme
         attributes are used here. -->
    <style name="Theme.MediaTracker" parent="android:Theme.Material.Light.NoActionBar" />
</resources>
