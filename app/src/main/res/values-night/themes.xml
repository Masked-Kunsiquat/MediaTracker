<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- See values/themes.xml — this is only the dark-mode counterpart of the bootstrap
         framework theme. Compose's MediaTrackerTheme owns real dark-mode theming. -->
    <style name="Theme.MediaTracker" parent="android:Theme.Material.NoActionBar" />
</resources>
