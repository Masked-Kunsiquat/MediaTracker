<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Ktor's OkHttp engine (okhttp-android) previously merged this permission into the final
         manifest transitively, so it was never declared here explicitly. Declaring it directly
         means a future engine swap (e.g. away from OkHttp) can't silently drop network access. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- android:allowBackup below reads misleadingly on its own, so: it does NOT mean "back this
         app up to Google Drive." Cloud backup is fully excluded in both rules files referenced
         here - every domain this app writes to is excluded, so that channel transfers nothing
         (ROADMAP Task 15 Phase B; AGENTS.md section 1's local-first, no-cloud premise). The
         attribute stays "true" only because it is the same switch that gates API 31+'s on-device
         device-transfer channel, which IS deliberately kept enabled, for the content-addressed
         cover images alone - see data_extraction_rules.xml's device-transfer section for why the
         covers travel and the database deliberately does not. Setting this to "false" would take
         that channel down too, not just cloud backup. -->
    <application
        android:name=".MediaTrackerApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MediaTracker">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.MediaTracker">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>