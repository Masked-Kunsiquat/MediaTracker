An empty field. In the CSV row, this is represented by no characters between the delimiters (e.g., `,,`). According to MediaTracker's data portability standards, "Empty means 'unknown'" for optional fields like `duration_seconds`, and placeholders such as `0`, `null`, `N/A`, or `-` MUST NOT be used.